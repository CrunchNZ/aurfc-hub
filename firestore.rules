rules_version='2';

service cloud.firestore {
  match /databases/{database}/documents {
    // DEVELOPMENT ONLY - Completely open access
    // WARNING: These rules allow anyone to read/write any data
    // Only use for development and testing
    
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
